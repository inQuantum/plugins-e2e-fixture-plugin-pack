{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "fixture-plugin-pack",
  "description": "A tiny throwaway plugin pack used to test Make's git plugin-pack import and pull mechanism."
}
